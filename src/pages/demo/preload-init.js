// 关键cgi数据预加载

(function() {
	// 直出页面
	// if (window.isSvr || !window.Preload) { return; }
	// const url = '/cgi-bin/getStudentEvaluateInfoList';
	// const preload = window.Preload;
	// if (!preload.timeStart) {
	//   preload.timeStart = {};
	// }
	// preload.timeStart[url] = (new Date()).getTime();
	// preload.load({
	//   url,
	//   type: 'GET',
	//   data: {
	//     start: 0,
	//     count: 10,
	//   },
	// });
})();
